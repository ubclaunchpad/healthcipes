import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  nutritionStyle: {
    flexDirection: 'column',
    borderRadius: 20,
    borderWidth: 1,
    padding: 10,
    alignItems: 'center',
    width: '20%',
  },
});
